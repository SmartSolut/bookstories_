@model Bookstore.Models.Author

@{
    ViewData["Title"] = "إضافة مؤلف جديد";
}

<div class="page-header">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-8">
                <h1 class="mb-0"><i class="fas fa-user-plus me-3"></i>إضافة مؤلف جديد</h1>
                <p class="mb-0 opacity-75">أدخل بيانات المؤلف الجديد</p>
            </div>
            <div class="col-md-4 text-md-end">
                <a asp-action="Index" class="btn btn-light btn-lg">
                    <i class="fas fa-arrow-right me-2"></i>العودة للقائمة
                </a>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-user-edit me-2"></i>بيانات المؤلف</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                        
                        <div class="mb-4">
                            <label asp-for="FullName" class="form-label fw-bold">
                                <i class="fas fa-user me-2"></i>الاسم الكامل للمؤلف
                            </label>
                            <input asp-for="FullName" class="form-control form-control-lg" 
                                   placeholder="أدخل الاسم الكامل للمؤلف" />
                            <span asp-validation-for="FullName" class="text-danger"></span>
                            <div class="form-text">
                                <i class="fas fa-info-circle me-1"></i>
                                أدخل الاسم الكامل للمؤلف كما يظهر على الكتب
                            </div>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-secondary btn-lg me-md-2">
                                <i class="fas fa-times me-2"></i>إلغاء
                            </a>
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-save me-2"></i>حفظ المؤلف
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Auto-focus on the first input field
        $(document).ready(function() {
            $('#FullName').focus();
        });
    </script>
} 